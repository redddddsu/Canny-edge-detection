#ifndef THRESHHOLD_HPP
#define THRESHHOLD_HPP

unsigned char* applyThreshhold(unsigned char* image, int width, int height);
unsigned char* edgeLinking(unsigned char* image, int width, int height);

#endif 